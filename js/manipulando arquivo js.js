// var y = 0;  COMENTÁRIO EM LINHA

/* var y = 2;
const x = 3;

comentário em grande escala = lembrar de abrir com /* e fechar com */

// var preco = 2; // VARIÁVEL É UM VALOR QUE SEMPRE MUDA. CONSTANTE É UM VALOR QUE NÃO MUDA.
// var desconto = 0.2;
// const PRECO = 2;   // CONSTANTE É DECLARADA EM CAIXA ALTA POR BOAS PRÁTICAS, PARA QUANDO OUTRAS PESSOAS LEREM, SABER QUE AQUILO É UMA CONSTANTE.
// var total = PRECO - desconto;

// FUNÇÕES

// function soma(a, b **Entre esses parenteses é colocado os atributos da função.){
//     return a + b;
// }

// soma(3, 5);

// CONSOLE

function returnEventValues(array) {
  for (let i = 0; i < array.length; )
    if (array[i] % 2 === 0) {
      evenNums.push(array[i]);
    }
}

console.log(evenNums);
